---
const { id } = Astro.params;
import PokeSingle from "@/components/PokeInfo.tsx";
import Layout from "@/layouts/Layout.astro";

const currentPage = Number(id);
---

<Layout title={`Pokemon ${currentPage}`}>
  <PokeSingle id={currentPage} client:only="react" />
</Layout>
